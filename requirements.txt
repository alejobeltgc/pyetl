pandas>=2.0.0
openpyxl>=3.1.0
pytest>=7.0.0

# AWS Lambda dependencies
# Note: boto3 and botocore are available in Lambda runtime
# Only include these for local development
boto3>=1.34.0
botocore>=1.34.0
